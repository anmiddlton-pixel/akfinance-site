// ── Bitrix24: приём заявок с форм сайта akfinance.ae ──────────────────
// НАСТРАИВАЕТ ОТДЕЛ РАЗРАБОТКИ.
// Вариант 1 (просто): вставить URL входящего вебхука Bitrix24 (право CRM) ниже —
//   формы со всех страниц начнут слать лиды в crm.lead.add. Пример:
//   https://портал.bitrix24.ru/rest/1/xxxxxxxxxxxxxxxx/
// Вариант 2 (безопаснее): завести серверный/serverless-прокси и слать на него
//   (тогда URL вебхука не светится в коде; антиспам). Тогда поправить sub() в HTML.
// Пока строка пустая — Bitrix не используется, работает запасной канал ниже (почта).
window.BITRIX_WEBHOOK = '';

// ── Запасной канал: заявка письмом через FormSubmit.co (без бэкенда) ──
// Работает, ПОКА BITRIX_WEBHOOK пуст. Заявка уходит письмом на этот адрес.
// ВАЖНО: при первой заявке FormSubmit пришлёт на этот ящик письмо со ссылкой
//   активации — её нужно один раз открыть, иначе письма не пойдут.
// Поменять получателя — просто впишите другой email.
window.FORM_EMAIL = 'info@aksystems.co';
